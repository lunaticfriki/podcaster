# Podcaster mock-server

First of all, install the dependencies:

```
pnpm install
```

To run the development server and the mock-server:

```
pnpm dev
```

To run the linter: 

```
pnpm lint
```

To build the project:

```
pnpm 
```

To run the typescript compiler:

```
pnpm tsc
```